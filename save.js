/*describe('arrayPush test', () => {
  test('should be',()=>{
    const list = [9,'u',1,11,3];
    const expectedResult = [9,'u',1,11];
    const result = arrayPush(list);
  expect (result).toBe(expectedResult);
  });
});

describe('given array push',() => {
  test('should be', () => {
    const data = [];
    const result = arraypush();
  })
})
export const arrayPush = (b)=>{
  let count = 0;
  const newArray = b[count]

  // eslint-disable-next-line no-unused-vars
  for (const i of b) { 
    count++ 

  }

;
  
  return newArray
}

if (a !== Array) {
  throw new Error('');
}
[9,'u',1,11,3]*/
